.task-container{id: "task-#{task.id}"}
  = link_to task.title, edit_task_path(task.id), class: 'title edit-link'
  %span.due-date
    %strong
      Due Date:
    = task.due_date ? task.due_date.strftime("%m/%d/%Y") : 'Not Set'